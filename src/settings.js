import React, {Component} from 'react'

import './settings.css'

const Settings = (props) => {
    return (
        <div id='settings-wrapper'>
            test modal
        </div>
    )
};

export default Settings;